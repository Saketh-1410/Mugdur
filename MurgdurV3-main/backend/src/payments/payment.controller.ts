// Razorpay removed — payment is handled via WhatsApp concierge flow.
// This file is kept as a placeholder so the module doesn't break.
import { Controller } from '@nestjs/common'

@Controller('payments')
export class PaymentController {}
